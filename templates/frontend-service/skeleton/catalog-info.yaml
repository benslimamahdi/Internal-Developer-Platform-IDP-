apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: ${{ values.name }} frontend service
  annotations:
    github.com/project-slug: benslimamahdi/${{ values.name }}
spec:
  type: website
  lifecycle: experimental
  owner: ${{ values.owner }}
  system: default-system
